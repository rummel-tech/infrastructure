name: infrastructure_dashboard
description: Artemis Infrastructure Dashboard - secrets, builds, and service management.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.1
  intl: ^0.19.0
  rummel_blue_theme:
    path: ../../../resources/rummel_blue_theme

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0

flutter:
  uses-material-design: true
